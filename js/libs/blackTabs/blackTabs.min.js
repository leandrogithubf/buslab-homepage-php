!function($){$.fn.blackTabs=function(options){let settings=$.extend({tab:".blackTab",tabActive:0,tabContent:".blackTab-content"},options),tabElement=this.find(settings.tab),tabContentElement=this.find(settings.tabContent);for(let i=0;i<$(tabElement).length;i++)$(tabElement).filter(":nth-child("+(i+1)+")").attr("data-id",i);$(tabElement).attr("data-active","off"),this.find(settings.tab+"[data-id="+settings.tabActive+"]").attr("data-active","on");for(let i=0;i<$(tabContentElement).length;i++)$(tabContentElement).filter(":nth-child("+(i+1)+")").attr("data-id",i);return $(tabContentElement).attr("data-active","off"),this.find(settings.tabContent+"[data-id="+settings.tabActive+"]").attr("data-active","on"),$(tabElement).click((function(event){event.preventDefault();let tabID=$(this).attr("data-id");$(tabElement).filter("[data-active=on]").attr("data-active","off"),$(this).attr("data-active","on"),$(tabContentElement).filter("[data-active=on]").attr("data-active","off").hide(),$(tabContentElement).filter("[data-id="+tabID+"]").attr("data-active","on").show()})),this}}(jQuery);